# Core storage. Relative paths are resolved from the project working directory.
OUTPUT_DIR=./outputs/
JOB_DB_PATH=db/app_data.db

# OpenRouter is the single gateway for hosted text and image models.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_TEXT_MODEL=google/gemini-3.1-flash-lite
OPENROUTER_APP_NAME=LC AI Canvas
# Optional public deployment URL, used only for OpenRouter attribution headers.
OPENROUTER_SITE_URL=
# Keep provider-side data collection denied and require a zero-data-retention route.
OPENROUTER_ZDR=true

# Separate concurrency lane for external API jobs.
OPENROUTER_MAX_CONCURRENT=4
OPENROUTER_MAX_PER_USER_QUEUE=5

# GPT Image 2 may take several minutes for complex edits/reference workflows.
GPT_IMAGE_2_TIMEOUT_SECONDS=300
# Keep the overall job timeout at least 30 seconds longer than the API timeout.
OPENROUTER_JOB_TIMEOUT_SECONDS=330

# Local ComfyUI is required only for RMBG, See-Through, and ACE-Step.
COMFYUI_SERVER=127.0.0.1:8188
# Optional absolute paths used for upload cleanup and output discovery.
COMFY_INPUT_DIR=
COMFY_OUTPUT_DIR=
COMFY_HTTP_CONNECT_TIMEOUT=3
COMFY_HTTP_READ_TIMEOUT=10
COMFY_WS_CONNECT_TIMEOUT=5
COMFY_WS_IDLE_TIMEOUT=120

# Default queue lane used by local ComfyUI jobs.
MAX_PER_USER_QUEUE=5
MAX_PER_USER_CONCURRENT=1
JOB_TIMEOUT_SECONDS=180

# Beta gate. Leave BETA_PASSWORD empty only for an intentionally open local instance.
BETA_PASSWORD=
BETA_COOKIE_NAME=beta_auth
# Set true behind HTTPS in production.
COOKIE_SECURE=false

# Only trust X-Forwarded-For when the request came from one of these proxies.
# Leave empty when the app is reached directly. Example: 10.20.0.0/16,127.0.0.1/32
# Keep Uvicorn proxy-header rewriting disabled; the included launchers already do this.
TRUSTED_PROXY_CIDRS=

# Browser gallery identity migration. "compat" accepts an existing, strictly
# validated anon_id once and upgrades it to a signed cookie automatically.
# Change to "enforced" after the migration window to reject unsigned cookies.
PRINCIPAL_IDENTITY_MODE=compat
# Prefer an explicit long random secret in managed deployments. When empty, the
# app creates and reuses db/principal_cookie.secret on this single host.
PRINCIPAL_COOKIE_SECRET=
# Optional path for the generated/read secret. Leave empty to use
# db/principal_cookie.secret on a single-host deployment.
PRINCIPAL_COOKIE_SECRET_FILE=
# Disabled by default; enabling this reopens caller-controlled impersonation.
ALLOW_LEGACY_ANON_HEADER=false

# Admin APIs fail closed unless both values are configured. The unsafe bypass
# exists only for isolated local development and should remain false.
ADMIN_USER=
ADMIN_PASSWORD=
ADMIN_ALLOW_UNAUTHENTICATED=false

# Upload, health, and API rate limits.
INPUTS_MAX_BYTES=10485760
INPUTS_MAX_PIXELS=40000000
# Compatibility escape hatch while legacy folder-scan removal is being canaried.
# Run `python -m app.asset_admin catalog-canary`; keep true until backup/restore gates pass.
ASSET_CATALOG_FALLBACK_ENABLED=true
HEALTHZ_DISK_MIN_FREE_MB=512
# Per-IP generation submissions per minute. Zero disables this limiter.
GEN_RATE_LIMIT_PER_MIN=0
# Translate Korean prompts to English through the configured text model.
ENABLE_PROMPT_TRANSLATE=true

# Application logs. JSON is recommended for production collection.
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_TO_FILE=false
LOG_FILE_PATH=logs/app.log
LOG_MAX_BYTES=1048576
LOG_BACKUP_COUNT=3
PROGRESS_LOG_STEP=10
PROGRESS_LOG_MIN_MS=500
PROGRESS_LOG_LEVEL=info

# Generation controls. Zero means unlimited/disabled for numeric limits.
GENERATION_ENABLED=true
MCP_GENERATION_ENABLED=true
GENERATION_CONTROL_TIMEZONE=Asia/Seoul
GENERATION_DAILY_REQUEST_LIMIT=0
GENERATION_DAILY_COST_LIMIT_USD=0
GENERATION_COST_CONFIRMATION_THRESHOLD_USD=0
# Optional estimated USD cost map used for reservation/confirmation decisions.
# Keys may be model, model|size, model|quality, model|size|quality, or capability:<name>.
GENERATION_COST_ESTIMATES_JSON={}

# MCP is expected to be restricted by the company network at the proxy/firewall.
# Optional second application-level allowlist. Empty means no additional filter.
# Example: 10.0.0.0/8,192.168.0.0/16,127.0.0.1/32,::1/128
MCP_ALLOWED_CLIENT_CIDRS=
# Absolute origin used in returned output URLs. Leave empty to derive it from the request.
# Example: https://ai-canvas.internal.example.com
MCP_PUBLIC_BASE_URL=
